import React from 'react';
import preloader from '../../../assets/images/Growingring.gif'

let Preloader = (props) => {
    return <div>
         <img src={preloader} />
    </div>
};

export default Preloader;